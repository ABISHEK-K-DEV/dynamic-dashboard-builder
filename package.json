{
  "name": "dynamic-dashboard-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Dynamic Dashboard Builder — scripts only; dependencies live in client/ and server/",
  "scripts": {
    "install:all": "npm install --prefix client --legacy-peer-deps && npm install --prefix server",
    "client": "npm run dev --prefix client",
    "server": "npm start --prefix server",
    "start": "npx --yes concurrently -n client,server -c blue,green \"npm run dev --prefix client\" \"npm run dev --prefix server\""
  }
}
